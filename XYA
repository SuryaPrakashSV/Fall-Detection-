Hi Akash, I checked the complete Query 5 code. In its final output, it renames `ACTUAL_EFFORT` to `ACTUAL_HOURS`, `RECORD_TYPE` to `ACTUAL_OR_FORECAST`, and `GRANULARITY_LEVEL` to `BREAKDOWN_LEVEL`.

Query 6 reads `DEMAND_FORECAST_NEW`, but it still uses the old column names. I tested Query 6’s calculation with the new names, and it returned results for 18 project types.

Can I update Query 6 to use the new column names? I have not changed the code yet.

