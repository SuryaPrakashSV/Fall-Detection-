cd "$HOME/Documents/wrike_project_status_history/full_implementation"

Then run:

python3 -c "
import os, json, getpass
from wrike_status_api import WrikeClient
tok = os.getenv('WRIKE_TOKEN') or getpass.getpass('Wrike token: ')
client = WrikeClient(tok)
data = client.get('folders/MQAAAAEGPpOb/comments')
print(json.dumps(data, indent=2))
" > comments_raw.json
