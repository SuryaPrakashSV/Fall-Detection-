First, confirm the values:

SELECT
    ACTUAL_OR_FORECAST,
    BREAKDOWN_LEVEL,
    COUNT(*) AS ROW_COUNT
FROM GBI_RETAIL_BAP_DB.ASO_OPS_WSA_SNDBX_BIZ_APP.DEMAND_FORECAST_NEW
GROUP BY 1, 2
ORDER BY 1, 2;

If the results contain ACTUAL and BY_PROJECT_TYPE, replace the existing project_actuals CTE in 06_SUPPLY_FORECAST_NEW.SQL with:

project_actuals AS (
    SELECT
        PROJECT_TYPE,
        SUM(ACTUAL_HOURS) AS total_historical_actual_hours
    FROM GBI_RETAIL_BAP_DB.ASO_OPS_WSA_SNDBX_BIZ_APP.DEMAND_FORECAST_NEW
    WHERE UPPER(ACTUAL_OR_FORECAST) = 'ACTUAL'
      AND UPPER(BREAKDOWN_LEVEL) = 'BY_PROJECT_TYPE'
      AND PROJECT_TYPE != 'ALL'
    GROUP BY PROJECT_TYPE
),
